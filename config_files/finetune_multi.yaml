exp: TextClassification

mode: 
    - train

models:
    hf_text_classification_model:
      - google-bert/bert-base-multilingual-cased

trainfpath: ../norm_data/train_filtered.tsv

validfpath: ../norm_data/validate_filtered.tsv

modelfpath: ../multi_model

loadPretrained: True
